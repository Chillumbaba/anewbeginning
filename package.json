{
  "name": "anewbeginning",
  "version": "1.0.0",
  "description": "A full-stack web application with React frontend and Node.js backend",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npm run build && mkdir -p public && cp -r ../client/build/* public/",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 